<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.merakisan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".company.CompanyTrendingActivity"></activity>
        <activity android:name=".company.UploadRequirementsActivity" />
        <activity android:name=".company.CompanyRegisterActivity" />
        <activity android:name=".company.CompanyLoginActivity" />
        <activity android:name=".company.CompanyProfileActivity" />
        <activity android:name=".company.HomePageActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
        <activity android:name=".farmer.TrendingActivity" />
        <activity android:name=".farmer.UploadProductActivity" />
        <activity android:name=".farmer.ProfileActivity" />
        <activity
            android:name=".farmer.HomeActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".farmer.RegisterActivity" />
        <activity android:name=".farmer.LoginActivity" />
        <activity android:name=".FirstActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>